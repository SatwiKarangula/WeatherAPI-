apiVersion: v1
kind: Service

metadata:
  name: weather-api-service

spec:
  selector:
    app: weather-api

  type: NodePort

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080